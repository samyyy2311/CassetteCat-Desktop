name: Format credential probe

on:
  push:
    branches: [harden/linux-credential-storage]
    paths:
      - 'src/services_scrobble.cpp'
      - 'tests/credential_vault/probe.cpp'
      - '.github/workflows/format-credential-probe.yml'

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: harden/linux-credential-storage
      - name: Install clang-format
        run: sudo apt-get update && sudo apt-get install -y clang-format
      - name: Format sources
        run: clang-format -i src/services_scrobble.cpp tests/credential_vault/probe.cpp
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name samyyy2311
          git config user.email samyyy2311@users.noreply.github.com
          git add src/services_scrobble.cpp tests/credential_vault/probe.cpp
          git commit -m "Format credential vault tests"
          git push origin HEAD:harden/linux-credential-storage
